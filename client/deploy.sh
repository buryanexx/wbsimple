#!/bin/bash

# Скрипт для деплоя клиентской части WB Simple
set -e

echo "🚀 Начинаем деплой клиентской части WB Simple..."

# Проверка наличия .env.production
if [ ! -f .env.production ]; then
    echo "⚠️ Файл .env.production не найден"
    echo "Создание .env.production из примера..."
    cp .env.example .env.production
    echo "⚠️ Не забудьте настроить переменные окружения в .env.production"
fi

# Установка зависимостей
echo "📦 Установка зависимостей..."
npm ci

# Сборка приложения
echo "🔧 Сборка приложения..."
npm run build

# Запуск приложения в режиме разработки (опционально)
echo "🔄 Запуск приложения в режиме разработки..."
echo "Для запуска в режиме разработки выполните: npm run dev"
echo "Для запуска в производственном режиме выполните: npm start"

echo "✅ Деплой клиентской части WB Simple успешно завершен!"
echo "📝 Веб-интерфейс будет доступен по адресу: http://localhost:3000" 